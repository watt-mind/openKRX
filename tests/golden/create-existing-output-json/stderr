openkrx: output.exists — something is already at the path --out names; nothing is ever overwritten, so name a file that does not exist yet, or move what is there out of the way first (exit 9)
openkrx: nothing had been written, so the destination is as it was found
