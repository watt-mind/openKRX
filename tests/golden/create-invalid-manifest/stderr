openkrx: manifest.invalid.unknown_field at field /attachments (attachment 0) — an attachments element carries a key the schema does not define. The key itself is not echoed, because you wrote it; that object's keys are path, file_name and description (exit 6)
openkrx: nothing had been written, so the destination is as it was found
