openkrx: output.exists at entry 0 — something is already at a path this run would create; nothing is ever overwritten, so extract into an empty directory, or name an --out file that does not exist yet, or move what is there out of the way first (exit 9)
openkrx: nothing had been written, so the destination is as it was found
